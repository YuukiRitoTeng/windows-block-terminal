name: TestDriver.ai Run

on:
    workflow_run:
        workflows: ["TestDriver.ai Build"]
        types:
            - completed
        branches:
            - main

env:
    NODE_VERSION: 22

permissions:
    actions: read
    contents: read
    id-token: write
    statuses: write

concurrency:
    group: testdriver-${{ github.event.workflow_run.id }}
    cancel-in-progress: false

jobs:
    run_testdriver:
        name: Run TestDriver.ai
        runs-on: windows-latest
        timeout-minutes: 40
        if: >-
            github.event.workflow_run.conclusion == 'success' &&
            github.event.workflow_run.event == 'workflow_dispatch'
        steps:
            - name: Checkout
              uses: actions/checkout@v7
              with:
                  ref: ${{ github.event.workflow_run.head_sha }}
                  persist-credentials: false

            - name: Setup Node.js
              uses: actions/setup-node@v7
              with:
                  node-version: ${{ env.NODE_VERSION }}

            - name: Authenticate to TestDriver
              uses: testdriverai/action@dae708d6ddf79ce83a3735de225336dc03d4c6e8
              with:
                  api-key: ${{ secrets.TD_API_KEY }}

            - name: Download, install, and launch Windows Block Terminal
              shell: pwsh
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
              run: |
                  $headers = @{
                      Authorization = "token $env:GITHUB_TOKEN"
                  }

                  $downloadFolder = "./download"

                  Write-Host "Starting the artifact download process..."

                  if (-not (Test-Path -Path $downloadFolder)) {
                      Write-Host "Creating download folder..."
                      New-Item -ItemType Directory -Path $downloadFolder | Out-Null
                  } else {
                      Write-Host "Download folder already exists."
                  }

                  Write-Host "Fetching the artifact download URL..."
                  $artifacts = (Invoke-RestMethod -Uri "https://api.github.com/repos/${{ github.repository }}/actions/runs/${{ github.event.workflow_run.id }}/artifacts" -Headers $headers).artifacts
                  $matchingArtifacts = @($artifacts | Where-Object { $_.name -eq "windows-exe" })
                  if ($matchingArtifacts.Count -ne 1) {
                      Write-Error "Expected exactly one windows-exe artifact, found $($matchingArtifacts.Count)."
                      exit 1
                  }
                  $artifactUrl = $matchingArtifacts[0].archive_download_url

                  if (-not $artifactUrl) {
                      Write-Error "windows-exe artifact was not found."
                      exit 1
                  }

                  $artifactZipPath = "$env:TEMP\artifact.zip"
                  try {
                      Invoke-WebRequest -Uri $artifactUrl `
                          -Headers $headers `
                          -OutFile $artifactZipPath `
                          -MaximumRedirection 5
                      Write-Host "Artifact downloaded successfully to $artifactZipPath"
                  } catch {
                      Write-Error "Error downloading artifact: $_"
                      exit 1
                  }

                  $artifactUnzipPath = "$env:TEMP\artifact"
                  Write-Host "Unzipping the artifact to $artifactUnzipPath..."
                  try {
                      Expand-Archive -Path $artifactZipPath -DestinationPath $artifactUnzipPath -Force
                  } catch {
                      Write-Error "Failed to unzip artifact: $_"
                      exit 1
                  }

                  $artifactInstallerPath = Get-ChildItem -Path $artifactUnzipPath -Filter *.exe -Recurse | Select-Object -First 1

                  if (-not $artifactInstallerPath) {
                      Write-Error "Executable file not found. Exiting."
                      exit 1
                  }

                  Write-Host "Running the installer: $($artifactInstallerPath.FullName)..."
                  try {
                      $installerProcess = Start-Process -FilePath $artifactInstallerPath.FullName -Wait -PassThru
                  } catch {
                      Write-Error "Failed to run the installer: $_"
                      exit 1
                  }
                  if ($installerProcess.ExitCode -ne 0) {
                      Write-Error "Installer exited with code $($installerProcess.ExitCode)."
                      exit 1
                  }

                  $appPath = Join-Path $env:LOCALAPPDATA "Programs\Windows Block Terminal\WindowsBlockTerminal.exe"
                  if (-not (Test-Path $appPath)) {
                      Write-Error "Windows Block Terminal executable was not found at $appPath"
                      exit 1
                  }

                  Write-Host "Launching the application: $appPath"
                  $appProcess = Start-Process -FilePath $appPath -PassThru
                  Start-Sleep -Seconds 5
                  if ($appProcess.HasExited) {
                      Write-Error "Windows Block Terminal exited immediately with code $($appProcess.ExitCode)."
                      exit 1
                  }

            - name: Run onboarding test
              shell: pwsh
              env:
                  FORCE_COLOR: "3"
              run: npx --yes testdriverai@latest run testdriver/onboarding.yml
