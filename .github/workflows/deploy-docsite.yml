name: Docsite CI

run-name: Build Docsite

env:
    NODE_VERSION: 22

on:
    push:
        branches:
            - main
    workflow_dispatch:
    # Also run any time a PR is opened targeting the docs
    pull_request:
        branches:
            - main
        types:
            - opened
            - synchronize
            - reopened
            - ready_for_review
        paths:
            - "docs/**"
            - ".github/workflows/deploy-docsite.yml"
            - "Taskfile.yml"

jobs:
    build:
        name: Build Docsite
        runs-on: ubuntu-latest
        if: github.event.pull_request.draft == false
        steps:
            - uses: actions/checkout@v7
              with:
                  fetch-depth: 0
            - uses: actions/setup-node@v7
              with:
                  node-version: ${{env.NODE_VERSION}}
                  cache: npm
                  cache-dependency-path: package-lock.json
            - name: Install Task
              uses: arduino/setup-task@v3
              with:
                  version: 3.x
                  repo-token: ${{ secrets.GITHUB_TOKEN }}
            - uses: nick-fields/retry@v4
              name: npm ci
              with:
                  command: npm ci --no-audit --no-fund
                  retry_on: error
                  max_attempts: 3
                  timeout_minutes: 5
            - name: Install isolated sharp for Docusaurus ideal-image
              run: npm install --prefix "${{ runner.temp }}/docsite-sharp" --no-save --package-lock=false --no-audit --no-fund sharp@0.34.3
            - name: Build docsite
              env:
                  NODE_PATH: ${{ runner.temp }}/docsite-sharp/node_modules
              run: task docsite:build:public
